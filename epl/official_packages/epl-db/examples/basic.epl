Use "epl-db"

Create conn equal to open("official-demo.db")
Call create_table(conn, "items", Map with name = "TEXT")
Call insert(conn, "items", Map with name = "demo")
Print to_text(query(conn, "SELECT * FROM items"))
Call close(conn)
